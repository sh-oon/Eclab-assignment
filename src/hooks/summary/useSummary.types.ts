// TypeScript type definitions for the summary array and ec_db
export type SummaryItem = {
  icon: string;
  title: string;
};

export type SummaryGroup = SummaryItem[];

export type Summary = SummaryGroup[];
